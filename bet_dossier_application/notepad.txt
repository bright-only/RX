in this section, i install the bootstrap.
I remove the boiler template with the  heading
I also install the router that will enable us to move from one component to another
create a folder called components and add js file called Home,Admin,Cashier and FundRequest.
Add content to the components.
Import all the component to app.js