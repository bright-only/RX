create file a variable to store the backend api url
import it in the component such as admin, cashier etc
create a constructor that take props as parameter
inside the constructor call the super method with the props as parameter, then write this.state={cashiers:[]}
the create table for the cashier